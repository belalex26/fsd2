include ../submenu/submenu

mixin menu(data)
  nav.menu
    span.menu__icon
    ul.menu__list
      each item in data.items
        li.menu__item
          a.menu__link= item.title
          if item.sublevel
            span.menu__sublevel-icon
            .menu__sublevel-content
              +submenu(item)